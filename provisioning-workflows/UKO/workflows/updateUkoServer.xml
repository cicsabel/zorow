<?xml version="1.0" encoding="UTF-8"?>

<!-- Declare external file containing WAS variables referenced in the following steps -->
<!DOCTYPE workflow [<!ENTITY variables SYSTEM "variable_imports.xml">
                    <!ENTITY step_stop_server SYSTEM "steps/stop_server.xml">
                    <!ENTITY step_update_server_directories SYSTEM "steps/update_server_directories.xml">
                    <!ENTITY step_start_server SYSTEM "steps/start_server.xml">
                    <!ENTITY step_update_server_parameters SYSTEM "steps/update_server_parameters.xml">
                    <!ENTITY step_update_server_create_versions SYSTEM "steps/update_server_create_versions.xml">
                    ]> 
                    
<!--
/******************************************************************************/
/* Copyright Contributors to the zOS-Workflow Project.                        */
/* SPDX-License-Identifier: Apache-2.0                                        */
/******************************************************************************/
-->
<workflow xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="schemas/workflow_v1.xsd">
    <!--  Note: The schema workflow_v1.xsd is shipped with z/OSMF. To use the schema
          to validate this XML, modify the above schema location to point to the 
          schema in z/OSMF, or copy the schema to an appropriate location.  -->

    <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
    <!-- UKO Provisioning : Update a new server                   -->
    <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->

  <workflowInfo>
      <workflowID>uko_Update</workflowID>
      <workflowDescription>Update a UKO server.</workflowDescription>
    <workflowVersion>1.0.0.0</workflowVersion>
        <!-- Build timestamp 2021.05.09.21.03.24 -->
      <vendor>IBM</vendor>
      <Provisioning>
          <productID>5655-EKM</productID>
          <productName>UKO</productName>
          <productVersion>V2R1+</productVersion>
          <softwareType>ukoserver</softwareType>
      </Provisioning>
  </workflowInfo>
  
  	<!--+++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
	<!--     Include variables used by the following steps     -->
	<!--+++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
	&variables;

    <variable name="NEW_SERVER_VERSION" scope="instance" visibility="public">
        <label>NEW_SERVER_VERSION</label>
        <abstract>Server version after update</abstract>
        <description>Which version of UKO has been updated to?</description>
        <category>String Variables</category>
        <string />
    </variable>

	<atCreate name="SERVER_INSTALL_DIR" scope="instance" prompt="true" required="false"/>
	<atCreate name="START_SERVER" scope="instance" prompt="true" required="false"/>
    <atCreate name="UKO_VAULT_ID" scope="instance" prompt="true" required="false"/>
    <atCreate name="JAVA_HOME" scope="instance" prompt="true" required="false"/>
    <atCreate name="DB_LIBPATH" scope="instance" prompt="true" required="false"/>
    <atCreate name="DB_CLASSPATH" scope="instance" prompt="true" required="false"/>
    <!-- those variables don't need to be queried because they are provieded from the instance -->
    <!-- <atCreate name="SERVER_TLS_KEY_STORE_SERVER_CERT" scope="instance" prompt="true" required="false"/> -->
    <!-- <atCreate name="SERVER_OIDC_PROVIDER_CERT" scope="instance" prompt="true" required="false"/> -->
    <!-- <atCreate name="WLP_UNAUTHENTICATED_USER" scope="instance" prompt="true" required="false"/> -->

    <!--++++++++++++++++++++++-->
    <!--   Stop the server    -->
    <!--++++++++++++++++++++++-->  

    &step_stop_server;

    <!--++++++++++++++++++++++-->
    <!--   check versions     -->
    <!--++++++++++++++++++++++-->  

    &step_update_server_create_versions;
    
    <!--++++++++++++++++++++++++++-->
    <!--  Update the server dir   -->
    <!--++++++++++++++++++++++++++-->  
	
    &step_update_server_directories;
    &step_update_server_parameters;

    <step name="defineRoles">
        <title>Define security roles</title>
        <description>Define EJB roles required to operate UKO and grant access</description>

        <step name="defineRolesGeneral">
            <title>Define general UKO roles</title>
            <description>Defines the general roles across all vaults</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA[${createServerUpdateVariables.stepState} == "Complete"]]></expression>
                <description>Version variables must have been set</description>
                <targetStateSet>
                    <description>Skip this step if we are on V3 or higher</description>
                    <extendStateExpression>
                        <description>Skip if the current version is already V3 or higher</description>
                        <expression>${instance-SERVER_VERSION} >= "3.1.0.00"</expression>
                        <targetState>skipped</targetState>
                    </extendStateExpression>
                </targetStateSet>
            </condition>
            <variableValue name="UKO_VAULT_ADMIN_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_KEY_ADMIN_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_KEY_CUSTODIAN1_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_KEY_CUSTODIAN2_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_AUDITOR_GROUP" scope="instance" noPromptIfSet="false" required="false"/>        
            <instructions>Executes a REXX program to create EJB roles and grant access</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <fileTemplate substitution="true">rexx/defineServerRolesGeneral.rexx</fileTemplate>
                <submitAs maxRc="4">TSO-REXX-JCL</submitAs>
                <maxLrecl>1024</maxLrecl>
            </template>
        </step>

        <step name="defineRolesVault">
            <title>Define vault-specific UKO roles</title>
            <description>Defines the vault-specific roles</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA[${createServerUpdateVariables.stepState} == "Complete"]]></expression>
                <description>Version variables must have been set</description>
                <targetStateSet>
                    <description>Skip this step if we are on V3 or higher</description>
                    <extendStateExpression>
                        <description>Skip if the current version is already V3 or higher</description>
                        <expression>${instance-SERVER_VERSION} >= "3.1.0.00"</expression>
                        <targetState>skipped</targetState>
                    </extendStateExpression>
                </targetStateSet>
            </condition>
            <variableValue name="UKO_VAULT_ID" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_VAULT_ADMIN_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_KEY_ADMIN_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_KEY_CUSTODIAN1_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_KEY_CUSTODIAN2_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <variableValue name="UKO_AUDITOR_GROUP" scope="instance" noPromptIfSet="false" required="false"/>
            <instructions>Executes a REXX program to create EJB roles and grant access</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <fileTemplate substitution="true">rexx/defineServerRolesVault.rexx</fileTemplate>
                <submitAs maxRc="4">TSO-REXX-JCL</submitAs>
                <maxLrecl>1024</maxLrecl>
            </template>
        </step>
        
    </step>

    &step_start_server;

    <step name="cleanUpVariables">
        <title>Clean up variables</title>
        <description>Submit script to clean up temp variables</description>
        <runAsUser substitution="true">$!{instance-UKO_ADMIN_SERVER}</runAsUser>
        <approver substitution="true">$!{instance-UKO_APPROVER_SERVER}</approver>
        <instructions substitution="true">Submit shell script to execute step</instructions>
        <weight>1</weight>
        <autoEnable>true</autoEnable>
        <template>
            <inlineTemplate substitution="true">
            
                #Remove temporary files
                rm ${instance-TEMP_DIR}/zosmf-${_workflow-workflowKey}.properties
                if [ $? -gt 0 ]; then 
                    echo "no temp file needed to be deleted"
                fi

                ${instance-TEMP_DIR}/zosmf-${_workflow-workflowKey}_server.env
                if [ $? -gt 0 ]; then 
                    echo "no temp server.env needed to be deleted"
                fi

                ${instance-TEMP_DIR}/zosmf-${_workflow-workflowKey}_jvm.options
                if [ $? -gt 0 ]; then 
                    echo "no temp jvm.options needed to be deleted"
                fi
            </inlineTemplate>
            <submitAs>shell-JCL</submitAs>
        </template>
    </step>

</workflow>