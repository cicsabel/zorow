<?xml version="1.0" encoding="UTF-8"?>

<!-- Declare external file containing WAS variables referenced in the following steps -->
<!DOCTYPE workflow [<!ENTITY variables SYSTEM "variable_imports.xml">
                    <!ENTITY image_properties SYSTEM "extensions/image_properties.xml">
                    ]> 
                    
<!--
/******************************************************************************/
/* Copyright Contributors to the zOS-Workflow Project.                        */
/* SPDX-License-Identifier: Apache-2.0                                        */
/******************************************************************************/
-->
<workflow xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../workflow/schemas/workflow_v1.xsd">
    <!--  Note: The schema workflow_v1.xsd is shipped with z/OSMF. To use the schema
          to validate this XML, modify the above schema location to point to the 
          schema in z/OSMF, or copy the schema to an appropriate location.  -->

    <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
    <!-- UKO Provisioning : Provision a new server                   -->
    <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->

  <workflowInfo>
      <workflowID>uko_makeServerEnvWritable</workflowID>
      <workflowDescription>change permissions to UKO Server configuration</workflowDescription>
    <workflowVersion>1.0.0.0</workflowVersion>
      <vendor>IBM</vendor>
      <Provisioning>
          <productID>5655-EKM</productID>
          <productName>UKO</productName>
          <productVersion>V2R1+</productVersion>
          <softwareType>UKO</softwareType>
      </Provisioning>
  </workflowInfo>
  
  	<!--+++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
	<!--     Include variables used by the following steps     -->
	<!--+++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
	&variables;

    <step name="setDirectoryPermissions">
        <title>Set Directory Permissions</title>
        <description>Changes permission to 666 for server.env and jvm.properties and 755 for directories</description>
        <runAsUser substitution="true">$!{instance-UKO_ADMIN_SERVER}</runAsUser>
        <approver substitution="true">$!{instance-UKO_APPROVER_SERVER}</approver>
        <instructions substitution="true">Submit scipt to execute action</instructions>
        <weight>1</weight>
        <autoEnable>true</autoEnable>
        <template>
          <inlineTemplate substitution="true">

            echo "Changing permission bits"
            chmod 775 ${instance-SERVER_USER_DIR}
            chmod 775 ${instance-SERVER_USER_DIR}/servers
            chmod 775 ${instance-SERVER_USER_DIR}/servers/${instance-SERVER_STC_NAME}
            chmod 666 ${instance-SERVER_USER_DIR}/servers/${instance-SERVER_STC_NAME}/server.env
            chmod 666 ${instance-SERVER_USER_DIR}/servers/${instance-SERVER_STC_NAME}/jvm.options
            chmod -R 775 ${instance-SERVER_OUTPUT_DIR}/${instance-SERVER_STC_NAME}           

            echo "permission bits are now:"
            ls -l ${instance-SERVER_USER_DIR}/servers/${instance-SERVER_STC_NAME}

            echo "change ownership of output directory to TSOUSER group"
            chown -R  ${instance-SERVER_STC_USER}:TSOUSER ${instance-SERVER_OUTPUT_DIR}/${instance-SERVER_STC_NAME} 

          </inlineTemplate>
          <submitAs>shell-JCL</submitAs>
       	</template>
    </step>
    
</workflow>