<?xml version="1.0" encoding="UTF-8"?>
<!-- Declare external file containing WAS variables referenced in the following steps -->
<!DOCTYPE workflow [<!ENTITY variables SYSTEM "variable_imports.xml">
                    <!ENTITY step_stop_server SYSTEM "steps/stop_server.xml">
                    ]> 
                    
<!--
/******************************************************************************/
/* Copyright Contributors to the zOS-Workflow Project.                        */
/* SPDX-License-Identifier: Apache-2.0                                        */
/******************************************************************************/
-->
<workflow xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                          xsi:noNamespaceSchemaLocation="../workflow/schemas/workflow_v1.xsd">    
    <!--  Note: The schema workflow_v1.xsd is shipped with z/OSMF. To use the schema
          to validate this XML, modify the above schema location to point to the 
          schema in z/OSMF, or copy the schema to an appropriate location.  -->

<!-- 
	+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ 
 	+  UKO Deprovision: Deprovision a server.                                +
	+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ 
-->

  <workflowInfo>
      <workflowID>ukoserver_Deprovision</workflowID>
      <workflowDescription>Deprovision a UKO server.</workflowDescription>
    <workflowVersion>1.0.0.0</workflowVersion>
      <vendor>IBM</vendor>
      <Provisioning>
          <productID>5655-EKM</productID>
          <productName>UKO</productName>
          <productVersion>V2R1+</productVersion>
          <softwareType>UKO</softwareType>
      </Provisioning>
  </workflowInfo>

  <!--+++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
	<!--     Include variables used by the following steps     -->
	<!--+++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
	&variables;
    <variable name="SECURITY_ADMIN_HOME" visibility="private">
        <label>SECURITY_ADMIN_HOME</label>
        <abstract>Home of the security admin</abstract>
        <description>Home directory of the security administrator</description>
        <category>String Variables</category>
        <string/>
    </variable>

    <!--++++++++++++++++++++++-->
    <!--   Deprovision Steps  -->
    <!--++++++++++++++++++++++-->
    &step_stop_server;

    <step name="deprovisionServer">
        <title>Deprovision the UKO server</title>
        <description>Submit script to deprovision a UKO server.</description>
        
        <step name="deleteAndUnmountZFS">
	        <title>Delete and unmount ZFS</title>
	        <description>If created, delete and unmount the ZFS</description>
	        <runAsUser substitution="true">$!{instance-UKO_ADMIN_ZFS}</runAsUser>
	        <approver substitution="true">$!{instance-UKO_APPROVER_ZFS}</approver>
	        <instructions substitution="true">Run commands to delete the SAF certificate and keyring.</instructions>
	        <weight>1</weight>
	        <autoEnable>true</autoEnable>
	        <template>
	          <fileTemplate substitution="true">jcl/delete-zfs.jcl</fileTemplate>
	          <submitAs>JCL</submitAs>
	      	</template>
		</step>
		
        <step name="deleteInstanceDirectory">
	       <title>Delete instance directory</title>
	       <description>Delete the directory for this server instance</description>
	        <runAsUser substitution="true">$!{instance-UKO_ADMIN_SERVER}</runAsUser>
	        <approver substitution="true">$!{instance-UKO_APPROVER_SERVER}</approver>

			<instructions substitution="false">Submitting script to delete server.</instructions>
	       <weight>1</weight>
	       <autoEnable>true</autoEnable>
	       <template>
                <inlineTemplate substitution="true">
                if [ -d ${instance-SERVER_USER_DIR}/servers/${instance-SERVER_STC_NAME} ];
                then
                    echo "removing server directory ${instance-SERVER_USER_DIR}/servers/${instance-SERVER_STC_NAME}"
                    rm -rf ${instance-SERVER_USER_DIR}/servers/${instance-SERVER_STC_NAME}
                fi
                #if(${instance-SERVER_OUTPUT_DIR} &amp;&amp; ${instance-SERVER_OUTPUT_DIR} != "")
                        if [ -d ${instance-SERVER_OUTPUT_DIR}/${instance-SERVER_STC_NAME} ];
                        then
                            echo "removing output directory ${instance-SERVER_OUTPUT_DIR}/${instance-SERVER_STC_NAME}"
                            rm -rf ${instance-SERVER_OUTPUT_DIR}/${instance-SERVER_STC_NAME}
                        fi    
                #end
                echo "delete shared class cache files"
                rm /tmp/javasharedresources/C290M11F1A64_*_sharedcc_${instance-SERVER_STC_NAME}_G43L00 
                if [ $? -gt 0 ]; then 
                    echo "no class cache files needed to be deleted"
                fi
	           </inlineTemplate>
	           <submitAs>shell-JCL</submitAs>
	       </template>
	   </step>

    </step>
 
    <step name="deleteIcsfAccess">
	        <title>Delete ICSF Access</title>
	        <description>Delete access to the ICSF profiles in CSFSERV</description>
	        <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
	        <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
			<condition>
			    <expression><![CDATA[${stopServer_console.stepState}=="Complete" || ${stopServer_console.stepState} == "Skipped"]]></expression>
			    <description>Check that the issue_stop stepState is Complete</description>

			</condition>
        	<instructions>Executes a REXX program to delete the SAF certificate and keyring.</instructions>
        	<weight>1</weight>
        	<autoEnable>true</autoEnable>
        	<template>
            	<fileTemplate substitution="true">extensions/deleteIcsfAccess.rexx</fileTemplate>
            	<submitAs maxRc="0">TSO-REXX-JCL</submitAs>
            	<maxLrecl>1024</maxLrecl>
        	</template>
	</step>

    <step name="deleteDsnrAccess">
        <title>Delete DSNR access</title>
        <description>Delete access to DSNR RRSAF and DIST</description>        
        <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
        <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
        <variableValue name="DB_JCC_SSID" scope="instance" noPromptIfSet="false" required="false"/>
        <variableValue name="SERVER_STC_USER" scope="instance" noPromptIfSet="false" required="false"/>
        <instructions substitution="true">Submit jcl to execute step</instructions>
        <weight>1</weight>
        <autoEnable>true</autoEnable>
        <template>
            <fileTemplate substitution="true">extensions/deleteDsnrAccess.rexx</fileTemplate>
            <submitAs maxRc="0">TSO-REXX-JCL</submitAs>
            <maxLrecl>1024</maxLrecl>
        </template>
    </step>

    <step name="prepareDynamicDeletion">
        <title>Prepare dynamic scripts for vault role and access deletion</title>
        <description>Prepare the dynamic scripts for the deletion of dynamic role access and roles in the EJBROLE class</description>

        <step name="resolveSecurityAdminHome" optional="false">
            <title>Resolve security admin home</title>
            <description>save the home directory of the security admin in a variable</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <instructions substitution="true">Submit shell script to execute step</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <inlineTemplate substitution="true">
                    #if(${instance-UKO_ADMIN_SECURITY} &amp;&amp; ${instance-UKO_ADMIN_SECURITY} != "")
                        userid=${instance-UKO_ADMIN_SECURITY}
                    #else
                        userid=${_step-stepOwnerUpper}
                    #end   

                    if [ -d "/u/${userid}" ]  
                    then
                            logfolder="/u/${userid}"
                    else
                            useridLC=$(echo "${userid}" | tr '[:upper:]' '[:lower:]')
                            logfolder="/u/${useridLC}"
                    fi                    

                    echo "SECURITY_ADMIN_HOME ${logfolder}" >> ${instance-TEMP_DIR}/zosmf-${_workflow-workflowKey}.properties
                </inlineTemplate>
                <submitAs>shell-JCL</submitAs>
                <output substitution="true" needResolveConflicts="false">${instance-TEMP_DIR}/zosmf-${_workflow-workflowKey}.properties</output>
            </template>
        </step>        
        
        <step name="copyVaultDeletionScriptTemplate">
		    <title>Copy template of vault deletion script</title>
	        <description>Copy the deleteRolesVault.rexx template to zFS temporary directory</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA["1" == "1"]]></expression>
                <description>Should the step be executed, based on the UKO_VAULT_ID setting</description>
                <targetStateSet>
                    <description/>
                    <extendStateExpression>
                        <description>Skip if SAF_PROFILE_PREFIX == "EKMFWEB"</description>
                        <expression><![CDATA[ ${instance-SAF_PROFILE_PREFIX} == "EKMFWEB"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                    <!-- <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID == ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} == "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression> -->
                </targetStateSet>
            </condition>
	        <instructions substitution="true">Click Finish to copy the template</instructions>
	        <weight>1</weight>
	        <autoEnable>true</autoEnable>
	        <template>
	            <fileTemplate substitution="true">extensions/deleteRolesVault.rexx</fileTemplate>
	            <saveAsUnixFile substitution="true">${instance-TEMP_DIR}/${instance-SERVER_STC_NAME}-deleteRolesVaultTemplate.rexx</saveAsUnixFile>
	        </template>
        </step>

        <step name="createVaultDeletionScript" optional="false">
            <title>Create script to delete vault profiles</title>
            <description>Create the rexx to be executed</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA["1" == "1"]]></expression>
                <description>Should the step be executed, based on the UKO_VAULT_ID setting</description>
                <targetStateSet>
                    <description/>
                    <extendStateExpression>
                        <description>Skip if SAF_PROFILE_PREFIX == "EKMFWEB"</description>
                        <expression><![CDATA[ ${instance-SAF_PROFILE_PREFIX} == "EKMFWEB"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                    <!-- <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID == ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} == "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression> -->
                </targetStateSet>
            </condition>
            <instructions substitution="true">Submit shell script to execute step</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <fileTemplate substitution="true">scripts/createVaultDeletionScript.sh</fileTemplate>
                <submitAs>shell-JCL</submitAs>
            </template>
        </step>

    </step>

    <step name="deleteRoleAccess">
        <title>Delete Role Access</title>
        <description>Delete access to the roles in EJBROLE</description>

        <step name="deleteRoleAccessLegacy">
            <title>Delete access to roles for EKMF pre version 3</title>
            <description>Legacy, delete access to roles for EKMF Web Version 2.x before vaults were introduced.</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA[${stopServer_console.stepState}=="Complete" || ${stopServer_console.stepState} == "Skipped"]]></expression>
                <description>Check that the issue_stop stepState is Complete</description>
                <targetStateSet>
                    <description>Only execute if user ids were generated</description>
                    <extendStateExpression>
                        <description>Skip if CREATE_TECHNICAL_USERIDS is false</description>
                        <expression><![CDATA[ ${instance-CREATE_TECHNICAL_USERIDS} == "false"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                    <!-- <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID != ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} != "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression> -->
            </targetStateSet>
            </condition>
            <instructions>Executes a REXX program to delete access to the EJB roles.</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <fileTemplate substitution="true">extensions/deleteRoleAccessLegacy.rexx</fileTemplate>
                <submitAs maxRc="0">TSO-REXX-JCL</submitAs>
                <maxLrecl>1024</maxLrecl>
            </template>
        </step>

        <step name="deleteRoleAccessGeneral">
            <title>Delete access to general UKO roles</title>
            <description>Deletes the general roles across all vaults</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA["1" == "1"]]></expression>
                <description>Should the step be executed, based on the UKO_VAULT_ID setting</description>
                <targetStateSet>
                    <description/>
                    <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID == ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} == "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                </targetStateSet>
            </condition>
            <instructions>Executes a REXX program to delete EJB roles and grant access</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <fileTemplate substitution="true">extensions/deleteRoleAccessGeneral.rexx</fileTemplate>
                <submitAs maxRc="4">TSO-REXX-JCL</submitAs>
                <maxLrecl>1024</maxLrecl>
            </template>
        </step>

        <step name="deleteRoleAccessVault">
            <title>Delete access to vault-specific UKO roles</title>
            <description>Deletes the vault-specific role access</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <!-- <condition>
                <expression><![CDATA["1" == "1"]]></expression>
                <description>Should the step be executed, based on the UKO_VAULT_ID setting</description>
                <targetStateSet>
                    <description/>
                    <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID == ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} == "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                </targetStateSet>
            </condition> -->
            <variableValue name="UKO_VAULT_ID" scope="instance" noPromptIfSet="false" required="false"/>
            <instructions>Executes a REXX program to delete EJB roles and grant access</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <fileTemplate substitution="true">extensions/deleteRoleAccessVault.rexx</fileTemplate>
                <submitAs maxRc="4">TSO-REXX-JCL</submitAs>
                <maxLrecl>1024</maxLrecl>
            </template>
        </step>

        <step name="deleteRoleAccessDynamic">
            <title>Delete all dynamic role accesses that have been granted later</title>
            <description>Execute the script that has been build by the grantVaultAccess workflow to delete additional access rights</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA["1" == "1"]]></expression>
                <description>Should the step be executed, based on the UKO_VAULT_ID setting</description>
                <targetStateSet>
                    <description/>
                    <extendStateExpression>
                        <description>Skip if SAF_PROFILE_PREFIX == "EKMFWEB"</description>
                        <expression><![CDATA[ ${instance-SAF_PROFILE_PREFIX} == "EKMFWEB"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                    <!-- <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID == ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} == "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression> -->
                </targetStateSet>
            </condition>
            <instructions>Executes a REXX program to delete EJB role access</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>                                                                             
                <fileTemplate substitution="true">jcl/delete-vault-access.jcl</fileTemplate>
                <submitAs maxRc="12">JCL</submitAs>
            </template> 
        </step>        
        
	</step>

    <step name="deleteRoles">
        <title>Delete Roles</title>
        <description>Delete roles in EJBROLE</description>

        <step name="deleteRolesLegacy">
            <title>Delete roles for EKMF pre version 3</title>
            <description>Legacy, delete roles for EKMF Web Version 2.x before vaults were introduced.</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA[${stopServer_console.stepState}=="Complete" || ${stopServer_console.stepState} == "Skipped"]]></expression>
                <description>Check that the issue_stop stepState is Complete</description>
                <targetStateSet>
                    <description>Only execute if user ids were generated</description>
                    <extendStateExpression>
                        <description>Skip if CREATE_TECHNICAL_USERIDS is false</description>
                        <expression><![CDATA[ ${instance-CREATE_TECHNICAL_USERIDS} == "false"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                    <!-- <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID != ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} != "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression> -->
                    <extendStateExpression>
                        <description>Skip if SAF_PROFILE_PREFIX == "EKMFWEB"</description>
                        <expression><![CDATA[ ${instance-SAF_PROFILE_PREFIX} == "EKMFWEB"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
            </targetStateSet>
            </condition>
            <instructions>Executes a REXX program to delete access to the EJB roles.</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <fileTemplate substitution="true">extensions/deleteRolesLegacy.rexx</fileTemplate>
                <submitAs maxRc="0">TSO-REXX-JCL</submitAs>
                <maxLrecl>1024</maxLrecl>
            </template>
        </step>

        <step name="deleteRolesGeneral">
            <title>Delete general UKO roles</title>
            <description>Deletes the general roles across all vaults</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA["1" == "1"]]></expression>
                <description>Should the step be executed, based on the UKO_VAULT_ID setting</description>
                <targetStateSet>
                    <description/>
                    <extendStateExpression>
                        <description>Skip if SAF_PROFILE_PREFIX == "EKMFWEB"</description>
                        <expression><![CDATA[ ${instance-SAF_PROFILE_PREFIX} == "EKMFWEB"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                    <!-- <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID == ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} == "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression> -->
                </targetStateSet>
            </condition>
            <instructions>Executes a REXX program to delete EJB roles and grant access</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>
                <fileTemplate substitution="true">extensions/deleteRolesGeneral.rexx</fileTemplate>
                <submitAs maxRc="4">TSO-REXX-JCL</submitAs>
                <maxLrecl>1024</maxLrecl>
            </template>
        </step>

        <step name="deleteRolesDynamic">
            <title>Delete dynamic vault-specific UKO roles</title>
            <description>Deletes the vault-specific roles that have been created by the grantVaultAccess script</description>
            <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
            <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
            <condition>
                <expression><![CDATA["1" == "1"]]></expression>
                <description>Should the step be executed, based on the UKO_VAULT_ID setting</description>
                <targetStateSet>
                    <description/>
                    <extendStateExpression>
                        <description>Skip if SAF_PROFILE_PREFIX == "EKMFWEB"</description>
                        <expression><![CDATA[ ${instance-SAF_PROFILE_PREFIX} == "EKMFWEB"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression>
                    <!-- <extendStateExpression>
                        <description>Skip if UKO_VAULT_ID == ekmfwebv2</description>
                        <expression><![CDATA[ ${instance-UKO_VAULT_ID} == "ekmfwebv2"]]></expression>
                        <targetState>Skipped</targetState>
                    </extendStateExpression> -->
                </targetStateSet>
            </condition>
            <instructions>Executes a REXX program to delete EJB roles</instructions>
            <weight>1</weight>
            <autoEnable>true</autoEnable>
            <template>                                                                             
                <fileTemplate substitution="true">jcl/delete-vault-roles.jcl</fileTemplate>
                <submitAs maxRc="12">JCL</submitAs>
            </template> 
        </step>

    </step>    

	<step name="deleteServerProc">                                                                  
         <title>Delete Server Proc</title>                                                           
         <description>Delete the server proc</description>   
         <runAsUser substitution="true">$!{instance-UKO_ADMIN_TSO}</runAsUser>
	     <approver substitution="true">$!{instance-UKO_APPROVER_TSO}</approver>                   
     	 <instructions substitution="false">deleteProcs</instructions>                        
         <weight>5</weight>                                                                     
         <autoEnable>true</autoEnable>                                                          
         <template>                                                                             
           <fileTemplate substitution="true">jcl/delete-server-proc.jcl</fileTemplate>
           <submitAs maxRc="12">JCL</submitAs>
       </template>                                                                            
    </step>

    <step name="removeSAFSecurityProfiles">
        <title>Remove SAF security profiles</title>
        <description>Remove SAF security profiles created by this workflow</description>
        <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
	    <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
        <instructions substitution="true">Delete the appropriate SAF profiles created by this workflow</instructions>
        <weight>1</weight>
        <autoEnable>true</autoEnable>
       	<template>
           	<fileTemplate substitution="true">extensions/deleteSecurity.rexx</fileTemplate>
           	<submitAs maxRc="0">TSO-REXX-JCL</submitAs>
           	<maxLrecl>1024</maxLrecl>
       	</template>
    </step>

    <step name="removeAdditionalSecuritySetup">
		<title>Remove Additional Security Setup</title>
		<description>Remove additional security settings</description>
		<step name="removeKeyAccess">
	        <title>Key prefix deletion</title>
	        <description>Remove entry in CSFKEYS class</description>
	        <runAsUser substitution="true">$!{instance-UKO_ADMIN_SECURITY}</runAsUser>
	        <approver substitution="true">$!{instance-UKO_APPROVER_SECURITY}</approver>
        	<instructions>Executes a REXX program to remove the CSFKEYS profiles.</instructions>
        	<weight>1</weight>
        	<autoEnable>true</autoEnable>
        	<template>
            	<fileTemplate substitution="true">extensions/deleteKeyAccess.rexx</fileTemplate>
            	<submitAs maxRc="8">TSO-REXX-JCL</submitAs>
            	<maxLrecl>1024</maxLrecl>
        	</template>
		</step>
    </step>

    <step name="cleanUpTemp">
        <title>Clean up temporary files</title>
        <description>Submit script to clean up temp files</description>
        <runAsUser substitution="true">$!{instance-UKO_ADMIN_SERVER}</runAsUser>
        <approver substitution="true">$!{instance-UKO_APPROVER_SERVER}</approver>
        <instructions substitution="true">Submit shell script to execute step</instructions>
        <weight>1</weight>
        <autoEnable>true</autoEnable>
        <template>
            <inlineTemplate substitution="true">
            
                #Remove temporary files
                rm ${instance-TEMP_DIR}/${instance-SERVER_STC_NAME}*
                if [ $? -gt 0 ]; then 
                    echo "no temp file needed to be deleted"
                fi

                rm ${instance-SECURITY_ADMIN_HOME}/${instance-SERVER_STC_NAME}-deleteRoleAccess.rexx
                rm ${instance-SECURITY_ADMIN_HOME}/${instance-SERVER_STC_NAME}-vaults.log
                rm ${instance-SECURITY_ADMIN_HOME}/${instance-SERVER_STC_NAME}-deleteRolesVault.rexx
                rm ${instance-SECURITY_ADMIN_HOME}/${instance-SERVER_STC_NAME}-sortedvaults.log

                rm -f ${instance-SERVER_OUTPUT_DIR}/.classCache/javasharedresources/*${instance-SERVER_STC_NAME}*

                rm ${instance-TEMP_DIR}/zosmf-${_workflow-workflowKey}.properties
                if [ $? -gt 0 ]; then 
                    echo "no temp file needed to be deleted"
                fi
            </inlineTemplate>
            <submitAs>shell-JCL</submitAs>
        </template>
    </step>
    
</workflow>
